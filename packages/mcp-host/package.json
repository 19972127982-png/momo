{
  "name": "@echopet/mcp-host",
  "version": "0.1.0",
  "private": true,
  "description": "EchoPet MCP host —— spawn/连接 MCP server（stdio）+ MCP↔DeepSeek function calling bridge",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@echopet/agent-core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.8"
  }
}
