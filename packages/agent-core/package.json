{
  "name": "@echopet/agent-core",
  "version": "0.1.0",
  "private": true,
  "description": "EchoPet Agent 核心抽象 —— Agent / Router / MemoryStore / PromptBuilder 接口；具体实现在 apps/desktop 和 apps/web 各自的 IO 适配层",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.8"
  }
}
