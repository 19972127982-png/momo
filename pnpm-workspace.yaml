packages:
  - "apps/*"
  - "packages/*"

# pnpm 11+ requires explicit approval for package build scripts to run
# (security feature; without this electron binary won't download)
allowBuilds:
  better-sqlite3: true
  electron: true
  electron-winstaller: true
  esbuild: true

onlyBuiltDependencies:
  - better-sqlite3
  - electron
  - electron-winstaller
  - esbuild
