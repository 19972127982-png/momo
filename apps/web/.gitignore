# dependencies
/node_modules

# next.js
/.next
/out
next-env.d.ts

# env（密钥不要进仓库）
.env
.env.local
.env*.local

# misc
.DS_Store
*.tsbuildinfo

# 注意：public/live2d、public/cubism 是站点要用的 Live2D 资产，
# 需要随仓库提交、给 Vercel 构建用，故不在此忽略。
